# CSV to Excel Converter - SAP Report

โปรแกรมแปลงไฟล์ CSV จาก SAP เป็นไฟล์ Excel ที่มีการจัดรูปแบบสวยงาม (ยังไม่เสร็จ)

## คุณสมบัติ

- อ่านไฟล์ CSV จาก SAP (6 ไฟล์) และแปลงเป็น Excel
- จัดรูปแบบอัตโนมัติ (ฟอนต์, สี, ขนาด, การจัดวาง)
- รองรับข้อมูลที่มีรายการ product เพิ่มหรือลดได้ในแต่ละเดือน
- สร้างไฟล์ Excel พร้อม 6 sheets:
  - ต้นทุน_กลุ่มธุรกิจ
  - ต้นทุน_กลุ่มบริการ
  - ต้นทุน_บริการ
  - หมวดบัญชี_กลุ่มธุรกิจ
  - หมวดบัญชี_กลุ่มบริการ
  - หมวดบัญชี_บริการ

## ความต้องการของระบบ

```bash
# Python 3.6 หรือสูงกว่า
python3 --version

# ติดตั้ง libraries ที่จำเป็น
pip3 install pandas openpyxl
```

## ไฟล์ CSV ที่ต้องใช้

โปรแกรมต้องการไฟล์ CSV 6 ไฟล์ในรูปแบบ:

```
001_ต้นทุน_BU_[PERIOD] - [DATE].csv
001_ต้นทุน_Product_[PERIOD] - [DATE].csv
001_ต้นทุน_Product_Group_[PERIOD] - [DATE].csv
002_บัญชี_BU_[PERIOD] - [DATE].csv
002_บัญชี_Product_[PERIOD] - [DATE].csv
002_บัญชี_Product_Grop_[PERIOD] - [DATE].csv
```

โดย:
- `[PERIOD]` = MMYY (เช่น 1025 = เดือนตุลาคม ค.ศ. 2025)
- `[DATE]` = วันที่พิมพ์ในรูปแบบ DD-MM-YY

**ตัวอย่าง:**
```
001_ต้นทุน_BU_1025 - 10-11-68.csv
001_ต้นทุน_Product_1025 - 10-11-68.csv
...
```

## วิธีใช้งาน

### 1. วิธีแรก: ระบุ period โดยตรง

```bash
python3 csv_to_excel.py 1025
```

โปรแกรมจะ:
1. ค้นหาไฟล์ CSV ที่มี period = 1025
2. แปลงเป็น Excel และบันทึกเป็น `Report_NT_BU_สะสม_2510.xlsx`

### 2. วิธีที่สอง: ให้โปรแกรมตรวจจับ period อัตโนมัติ

```bash
python3 csv_to_excel.py
```

โปรแกรมจะค้นหาไฟล์ CSV ในโฟลเดอร์ปัจจุบันและตรวจจับ period โดยอัตโนมัติ

## โครงสร้างโฟลเดอร์

```
sap2excel/
├── csv_to_excel.py           # โปรแกรมหลัก
├── README.md                  # คู่มือนี้
├── 001_ต้นทุน_BU_1025 - 10-11-68.csv
├── 001_ต้นทุน_Product_1025 - 10-11-68.csv
├── 001_ต้นทุน_Product_Group_1025 - 10-11-68.csv
├── 002_บัญชี_BU_1025 - 10-11-68.csv
├── 002_บัญชี_Product_1025 - 10-11-68.csv
├── 002_บัญชี_Product_Grop_1025 - 10-11-68.csv
└── Report_NT_BU_สะสม_2510.xlsx  # ไฟล์ที่สร้างขึ้น
```

## การปรับแต่งโปรแกรม

### เปลี่ยนรูปแบบการจัดรูปแบบ

แก้ไขในส่วน `__init__` ของ class `CSVToExcelConverter`:

```python
# Styles
self.font_header = Font(name='TH Sarabun New', size=18, bold=True)
self.font_data = Font(name='TH Sarabun New', size=16)
self.fill_header = PatternFill(start_color='FFF4DEDC', end_color='FFF4DEDC', fill_type='solid')
```

### เปลี่ยนชื่อไฟล์ output

แก้ไขในส่วน `convert` method:

```python
# สร้างชื่อไฟล์ output
if not output_filename:
    month_num = period[:2]
    year_suffix = period[2:4]
    output_filename = f"Report_NT_BU_สะสม_{year_suffix}{month_num}.xlsx"
```

### เพิ่ม sheet ใหม่

แก้ไขใน `file_mapping`:

```python
self.file_mapping = {
    "001_ต้นทุน_BU": "ต้นทุน_กลุ่มธุรกิจ",
    "002_บัญชี_BU": "หมวดบัญชี_กลุ่มธุรกิจ",
    # เพิ่ม mapping ใหม่ที่นี่
}
```

## การแก้ไขปัญหา

### ปัญหา: ไม่พบไฟล์ CSV

```
❌ ไม่พบไฟล์ CSV สำหรับงวด 1025
```

**แก้ไข:**
- ตรวจสอบว่าไฟล์ CSV อยู่ในโฟลเดอร์เดียวกับ `csv_to_excel.py`
- ตรวจสอบชื่อไฟล์ว่าตรงกับรูปแบบที่กำหนด

### ปัญหา: Encoding error

```
UnicodeDecodeError: 'utf-8' codec can't decode...
```

**แก้ไข:**
- ไฟล์ CSV ต้องเป็น encoding `cp874` (Thai Windows)
- โปรแกรมจะจัดการเรื่องนี้อัตโนมัติ

### ปัญหา: ข้อมูลไม่ครบ

**แก้ไข:**
- ตรวจสอบว่าไฟล์ CSV มีข้อมูลครบถ้วน
- ตรวจสอบโครงสร้างของไฟล์ CSV ว่าถูกต้อง

## ตัวอย่างผลลัพธ์

```bash
$ python3 csv_to_excel.py 1025

🔍 ตรวจพบงวด: 1025 จากไฟล์ 001_ต้นทุน_BU_1025 - 10-11-68.csv
✓ พบไฟล์ CSV 6 ไฟล์
  - 001_ต้นทุน_BU_1025 - 10-11-68.csv → ต้นทุน_กลุ่มธุรกิจ
  - 001_ต้นทุน_Product_Group_1025 - 10-11-68.csv → ต้นทุน_กลุ่มบริการ
  - 001_ต้นทุน_Product_1025 - 10-11-68.csv → ต้นทุน_บริการ
  - 002_บัญชี_BU_1025 - 10-11-68.csv → หมวดบัญชี_กลุ่มธุรกิจ
  - 002_บัญชี_Product_Grop_1025 - 10-11-68.csv → หมวดบัญชี_กลุ่มบริการ
  - 002_บัญชี_Product_1025 - 10-11-68.csv → หมวดบัญชี_บริการ

⏳ กำลังประมวลผล: ต้นทุน_กลุ่มธุรกิจ...
  ✓ สำเร็จ

⏳ กำลังประมวลผล: ต้นทุน_กลุ่มบริการ...
  ✓ สำเร็จ

⏳ กำลังประมวลผล: ต้นทุน_บริการ...
  ✓ สำเร็จ

⏳ กำลังประมวลผล: หมวดบัญชี_กลุ่มธุรกิจ...
  ✓ สำเร็จ

⏳ กำลังประมวลผล: หมวดบัญชี_กลุ่มบริการ...
  ✓ สำเร็จ

⏳ กำลังประมวลผล: หมวดบัญชี_บริการ...
  ✓ สำเร็จ

✅ สร้างไฟล์สำเร็จ: Report_NT_BU_สะสม_2510.xlsx
   ตำแหน่ง: ./Report_NT_BU_สะสม_2510.xlsx
```

## การใช้งานในอนาคต

เมื่อมีไฟล์ CSV เดือนใหม่:

1. คัดลอกไฟล์ CSV ทั้ง 6 ไฟล์ใส่ในโฟลเดอร์
2. รันคำสั่ง:
   ```bash
   python3 csv_to_excel.py [period]
   ```
3. ไฟล์ Excel จะถูกสร้างอัตโนมัติพร้อมการจัดรูปแบบ

## สิ่งที่ต้องทราบ

- โปรแกรมจะแปลง period format `MMYY` เป็นข้อความภาษาไทยโดยอัตโนมัติ
  - เช่น: `1025` → "สำหรับงวด 10 เดือน สิ้นสุดวันที่ 31 ตุลาคม 2568"
- ข้อมูล product ที่เพิ่มหรือลดในแต่ละเดือนจะถูกจัดการโดยอัตโนมัติ
- การจัดรูปแบบจะคงสภาพเดิมตาม template

## ผู้พัฒนา

สร้างโดย Claude Code สำหรับ NT (บริษัท โทรคมนาคมแห่งชาติ จำกัด (มหาชน))

## License

ใช้ภายในองค์กรเท่านั้น
