โปรแกรมนี้เปรียบเสมือน **"ระบบตรวจสอบความผิดปกติอัตโนมัติ"** ครับ ซึ่งแบ่งการทำงานออกเป็น 3 ส่วนหลัก:

1.  **`main_audit.py` (ไฟล์ตั้งค่าและควบคุม):**

      * นี่คือไฟล์ที่คุณใช้ "สั่งงาน" โปรแกรมครับ
      * คุณต้องกำหนดค่าต่างๆ ที่นี่ เช่น:
          * ไฟล์ข้อมูลเข้า (`INPUT_FILE_LONG` หรือ `INPUT_FILE_CROSSTAB`)
          * ไฟล์ผลลัพธ์ (`OUTPUT_FILE`)
          * คอลัมน์เป้าหมายที่จะตรวจ (เช่น `TARGET_COL = "EXPENSE_VALUE"`)
          * เปิด/ปิด การวิเคราะห์แต่ละแบบ (เช่น `RUN_TIME_SERIES_ANALYSIS = True`)

2.  **`anomaly_engine.py` (เอนจิ้น/สมองกลวิเคราะห์):**

      * นี่คือ "หัวใจ" ของโปรแกรมที่ทำการคำนวณและค้นหาความผิดปกติทั้งหมด
      * เมื่อได้รับข้อมูลมา มันจะใช้วิธีการวิเคราะห์หลักๆ 2 รูปแบบเพื่อค้นหาปัญหา

3.  **`anomaly_reporter.py` (เครื่องมือสร้างรายงาน):**

      * หลังจากเอนจิ้นวิเคราะห์เสร็จ ส่วนนี้จะนำผลลัพธ์ที่ได้มาสร้างเป็นไฟล์ Excel
      * มันจะทาสีช่องที่ผิดปกติ, เพิ่มคำอธิบาย (Legend), และจัดรูปแบบให้อ่านง่าย

-----

### 🧠 หัวใจหลัก: 2 วิธีการวิเคราะห์ (Anomaly Engine)

นี่คือส่วนที่สำคัญที่สุดที่โปรแกรมของคุณทำครับ คือการตรวจหาความผิดปกติ 2 รูปแบบ โดยเอนจิ้นจะตรวจสอบข้อมูลของคุณ "ทุกเดือน" ด้วย 2 วิธีนี้

| | **วิธีที่ 1: Time Series (เทียบกับอดีต)** | **วิธีที่ 2: Peer Group (เทียบกับกลุ่ม)** |
| :--- | :--- | :--- |
| **โค้ดที่ใช้** | `audit_time_series_all_months` (ใช้ Rolling IQR) | `audit_peer_group_all_months` (ใช้ IsolationForest) |
| **หลักการ** | **"เทียบกับอดีตของตัวเอง"** | **"เทียบกับเพื่อนกลุ่มเดียวกัน"** |
| **คำถามที่ตอบ** | "หน่วยงานนี้ (`COST_CENTER`) ที่เบิก `GL_CODE` นี้... <br>...ยอดเดือนนี้มัน **สูงหรือต่ำ** ผิดปกติ <br>...เมื่อเทียบกับ **6 เดือนที่แล้ว** ของ *ตัวเอง* หรือไม่?" | "ในเดือนนี้ สำหรับ `GL_CODE` นี้... <br>...มี `COST_CENTER` ไหนที่ยอดเบิก **โดดเด่น** <br>...เมื่อเทียบกับ `COST_CENTER` *อื่นๆ* ที่เบิก `GL_CODE` เดียวกันหรือไม่?" |
| **ตัวอย่าง** | `COST_CENTER` A เบิกค่าไฟ (GL 5001) <br>6 เดือนที่แล้ว: 100, 105, 110, 95, 100, 105 <br>เดือนนี้: **500** <br>👉 **ผลลัพธ์:** **ผิดปกติ (High Spike)** | ในเดือนนี้ ทุก `COST_CENTER` เบิกค่าไฟ (GL 5001) <br>A: 100, B: 105, C: 110, D: 95, E: **500** <br>👉 **ผลลัพธ์:** **ผิดปกติ (High Outlier vs Peers)** |
| **การตั้งค่า** | `AUDIT_TS_DIMENSIONS`: บอกว่า "ตัวเอง" คือใคร (เช่น `GROUP_NAME` + `GL_CODE` + `COST_CENTER`) <br> `AUDIT_TS_WINDOW`: บอกว่าจะเทียบกับอดีตกี่เดือน (เช่น 6 เดือน) | `AUDIT_PEER_GROUP_BY`: บอกว่า "กลุ่มเพื่อน" คือใคร (เช่น `GROUP_NAME` + `GL_CODE`) <br> `AUDIT_PEER_ITEM_ID`: บอกว่า "สมาชิก" ในกลุ่มคือใคร (เช่น `COST_CENTER_DEPARTMENT`) |
| **จุดแข็ง** | • หา "พฤติกรรมที่เปลี่ยนไป" ของแต่ละหน่วยงานได้ดี <br> • เข้าใจ Trend (ถ้ายอดค่อยๆ ขึ้น จะไม่เตือน) | • หา "แกะดำ" หรือ "ตัวประหลาด" ในกลุ่มได้ดี <br> • แม้ยอดจะเท่าเดิม แต่ถ้าคนอื่นไม่ทำ ก็เจอได้ |

-----

### 📄 ผลลัพธ์ที่ได้: รายงาน Excel (Output File)

เมื่อโปรแกรมรันเสร็จ คุณจะได้ไฟล์ Excel ที่มี Sheet ต่างๆ ดังนี้:

1.  **`Crosstab_Report`** (สรุปภาพรวม - เทียบอดีต)

      * **นี่คือ Sheet หลัก:** มันคือตารางสรุปยอดของแต่ละ `GL_CODE` (หรือตามที่คุณตั้งค่า `CROSSTAB_DIMENSIONS`)
      * **การทาสี:** โปรแกรมจะคำนวณหาความผิดปกติของ "ทุกช่อง" ในตารางนี้ (โดยใช้หลักการ "เทียบกับอดีต" หรือ **วิธีที่ 1**)
      * **สีแดง (High\_Spike):** ยอดเดือนนี้พุ่งสูงกว่าในอดีตมาก
      * **สีเหลือง (Low\_Spike):** ยอดเดือนนี้ตกลงต่ำกว่าในอดีตมาก
      * **สีแดงทึบ (Negative\_Value):** ยอดติดลบ
      * **คอลัมน์ `ANOMALY_STATUS`:** จะสรุปสถานะของ **"เดือนล่าสุด"** เท่านั้น

2.  **`Peer_Crosstab_Report`** (สรุปภาพรวม - เทียบกลุ่ม)

      * อันนี้คล้ายกับ `Crosstab_Report` แต่ถูกสร้างขึ้นมาเพื่อไฮไลท์ปัญหาที่ **"วิธีที่ 2" (Peer Group)** ตรวจเจอโดยเฉพาะ
      * ถ้าช่องไหนถูกทาสี แปลว่า `COST_CENTER` นั้น มียอดใช้จ่าย "โดด" กว่าเพื่อนในเดือนนั้นๆ

3.  **`Full_Audit_Log (Time)`** (บันทึกผล chi tiết - เทียบอดีต)

      * นี่คือ "ผลตรวจโดยละเอียด" จาก **วิธีที่ 1 (Time Series)**
      * มันจะแสดง **"ทุกเดือน"** **"ทุกรายการ"** ที่โปรแกรมพบว่าผิดปกติ (เช่น ยอด 500 ในเดือน ม.ค., ยอด 600 ในเดือน มี.ค.)
      * ใช้สำหรับ: การตรวจสอบย้อนหลังแบบละเอียด

4.  **`Full_Audit_Log (Peer)`** (บันทึกผล chi tiết - เทียบกลุ่ม)

      * นี่คือ "ผลตรวจโดยละเอียด" จาก **วิธีที่ 2 (Peer Group)**
      * มันจะแสดง **"ทุกเดือน"** **"ทุกรายการ"** ที่เป็น "แกะดำ" เมื่อเทียบกับเพื่อน
      * ใช้สำหรับ: การตรวจสอบว่าใครทำตัวไม่เหมือนเพื่อนบ้าง

-----

### 💡 สรุปการนำไปใช้

1.  **ถ้าอยากดูภาพรวมเร็วๆ (พฤติกรรมเปลี่ยนไปไหม?):**
    ให้ไปที่ Sheet `Crosstab_Report` แล้วมองหา "สีแดง" หรือ "สีเหลือง"

2.  **ถ้าอยากรู้ว่ามีใครทำตัวแปลกกว่าคนอื่นไหม?:**
    ให้ไปที่ Sheet `Peer_Crosstab_Report` หรือ `Full_Audit_Log (Peer)`

3.  **ถ้าอยากเจาะลึกรายการที่น่าสงสัย (เช่น ที่เห็นสีแดงใน Crosstab):**
    ให้ Copy รายการนั้น (เช่น `GL_CODE` A, `COST_CENTER` B) แล้วไปค้นหาใน `Full_Audit_Log (Time)` เพื่อดูพฤติกรรมย้อนหลังของมันครับ

