# 🧹 Data Cleaning Guide - การทำความสะอาดข้อมูลตัวเลข

## 🎯 ความสามารถ

โปรแกรม `main_audit.py` และ `crosstab_converter.py` รองรับการทำความสะอาดข้อมูลตัวเลขในรูปแบบบัญชี:

✅ **รองรับรูปแบบ:**
- Comma (,): `3,000.00` → `3000.00`
- Parentheses (วงเล็บ = ค่าลบ): `(3000)` → `-3000`
- รวมกัน: `(30,000.00)` → `-30000.00`
- ช่องว่าง: `" 5000 "` → `5000`
- สกุลเงิน: `$1,000` หรือ `฿2,500` → `1000` / `2500`
- เปอร์เซ็นต์: `50%` → `50`

---

## 📋 ตารางตัวอย่าง

| รูปแบบเดิม | ผลลัพธ์ | คำอธิบาย |
|------------|---------|----------|
| `1000` | `1000.00` | ตัวเลขปกติ |
| `3,000` | `3000.00` | มี comma |
| `1,234,567` | `1234567.00` | หลาย comma |
| `3000.50` | `3000.50` | มีทศนิยม |
| `(3000)` | `-3000.00` | **วงเล็บ = ค่าลบ** (บัญชี) |
| `(30,000)` | `-30000.00` | วงเล็บ + comma |
| `(1,234,567.89)` | `-1234567.89` | วงเล็บ + comma + ทศนิยม |
| `" 5000 "` | `5000.00` | มีช่องว่าง |
| `$1,000` | `1000.00` | สกุลเงินดอลลาร์ |
| `฿2,500` | `2500.00` | สกุลเงินบาท |
| `50%` | `50.00` | เปอร์เซ็นต์ |
| `$ 10,000.00` | `10000.00` | รวมหลายรูปแบบ |
| `` (ว่าง) | `0.00` | ค่าว่าง → 0 |
| `.50` | `0.50` | ทศนิยมเท่านั้น |

---

## 🔧 ฟังก์ชันที่ใช้

### `clean_numeric_column(series)` ใน `main_audit.py`

```python
def clean_numeric_column(series):
    """
    ทำความสะอาดคอลัมน์ตัวเลข รองรับรูปแบบบัญชี

    รองรับ:
    - Comma: 3,000.00 → 3000.00
    - Parentheses (negative): (3000) → -3000
    - Combined: (30,000.00) → -30000.00
    - Whitespace: " 3000 " → 3000
    - Currency: $3,000 หรือ ฿3,000 → 3000
    """
    # แปลงเป็น string
    s = series.astype(str)

    # ตรวจสอบวงเล็บ (ค่าลบในระบบบัญชี)
    is_negative = s.str.contains(r'\(.*\)', regex=True, na=False)

    # ลบอักขระพิเศษ (เว้น . และ -)
    s = s.str.replace(r'[,\(\)\s$฿%]', '', regex=True)

    # แปลงเป็นตัวเลข
    s = pd.to_numeric(s, errors='coerce').fillna(0)

    # ใส่เครื่องหมายลบสำหรับค่าที่อยู่ในวงเล็บ
    s.loc[is_negative] = -s.loc[is_negative].abs()

    return s
```

---

## 💡 วิธีการทำงาน

### ขั้นตอนที่ 1: ตรวจสอบวงเล็บ
```python
is_negative = s.str.contains(r'\(.*\)', regex=True, na=False)
```
- ตรวจสอบว่าค่าไหนอยู่ในวงเล็บ `(3000)`
- เก็บค่า `True/False` ไว้ใน `is_negative`

### ขั้นตอนที่ 2: ลบอักขระพิเศษ
```python
s = s.str.replace(r'[,\(\)\s$฿%]', '', regex=True)
```
- ลบ: `,` `(` `)` ช่องว่าง `$` `฿` `%`
- `(30,000)` → `30000`
- `$1,000` → `1000`

### ขั้นตอนที่ 3: แปลงเป็นตัวเลข
```python
s = pd.to_numeric(s, errors='coerce').fillna(0)
```
- แปลงเป็น float
- ถ้าแปลงไม่ได้ → `NaN` → แทนที่ด้วย `0`

### ขั้นตอนที่ 4: ใส่เครื่องหมายลบ
```python
s.loc[is_negative] = -s.loc[is_negative].abs()
```
- ค่าที่อยู่ในวงเล็บ (จาก step 1) → คูณ -1
- `30000` → `-30000`

---

## 🧪 ตัวอย่างการใช้งาน

### ตัวอย่างที่ 1: ข้อมูล Expense

#### Before:
```csv
GL_CODE,GL_NAME,EXPENSE_VALUE
51642102,ค่าซ่อมแซม-อาคาร,"24,972.44"
51642103,ค่าซ่อมแซม-ชุมสาย,"(1,503,671.96)"
51642104,ค่าซ่อมแซม-เสา,"41,208,496.98"
51642105,ค่าซ่อมแซม-อุปกรณ์,"21,023,087.60"
```

#### After Cleaning:
```csv
GL_CODE,GL_NAME,EXPENSE_VALUE
51642102,ค่าซ่อมแซม-อาคาร,24972.44
51642103,ค่าซ่อมแซม-ชุมสาย,-1503671.96
51642104,ค่าซ่อมแซม-เสา,41208496.98
51642105,ค่าซ่อมแซม-อุปกรณ์,21023087.60
```

---

### ตัวอย่างที่ 2: ข้อมูล Revenue

#### Before:
```csv
SERVICE_CODE,SERVICE_NAME,REVENUE_VALUE
SRV001,Internet,$1,234.56
SRV002,Mobile,"฿2,500.00"
SRV003,Refund,"(500)"
```

#### After Cleaning:
```csv
SERVICE_CODE,SERVICE_NAME,REVENUE_VALUE
SRV001,Internet,1234.56
SRV002,Mobile,2500.00
SRV003,Refund,-500.00
```

---

## ⚙️ การทำงานใน main_audit.py

ฟังก์ชัน `prepare_data()` จะเรียกใช้ `clean_numeric_column()` อัตโนมัติ:

```python
def prepare_data(df):
    # ...

    # 2. แปลงตัวเลข (รองรับรูปแบบบัญชี: comma, วงเล็บ)
    if TARGET_COL in df.columns:
        df[TARGET_COL] = clean_numeric_column(df[TARGET_COL])
        print(f"   ✓ Converted {TARGET_COL} to numeric (accounting format supported)")

    # ...
```

---

## ⚙️ การทำงานใน crosstab_converter.py

ฟังก์ชัน `clean_data()` จะเรียกใช้ `clean_numeric_value()` อัตโนมัติ:

```python
def clean_data(self, value_col='VALUE'):
    # แปลงค่าเป็นตัวเลข (รองรับรูปแบบบัญชี)
    self.df_long[value_col] = self.clean_numeric_value(self.df_long[value_col])

    # ลบแถวที่ไม่มีค่า
    self.df_long = self.df_long.dropna(subset=[value_col])
```

---

## 🎯 สิ่งที่รองรับ vs ไม่รองรับ

### ✅ รองรับ:
- Comma: `3,000`
- วงเล็บ (ค่าลบ): `(3000)`
- ทศนิยม: `3000.50`
- สกุลเงิน: `$`, `฿`
- ช่องว่าง: `" 3000 "`
- เปอร์เซ็นต์: `50%`
- รวมกัน: `(30,000.00)`

### ❌ ไม่รองรับ:
- สกุลเงินอื่นๆ: `€`, `¥`, `£` (ต้องเพิ่มใน regex)
- รูปแบบวันที่: `2025-01-01` (ใช้คอลัมน์วันที่แทน)
- ตัวอักษรปนตัวเลข: `3K`, `5M`, `10B`

---

## 🔍 ตรวจสอบผลลัพธ์

หลังจากรัน `main_audit.py` จะเห็นข้อความ:

```
   running: Data Preprocessing...
   ✓ Created date from YEAR, MONTH columns
   ✓ Converted EXPENSE_VALUE to numeric (accounting format supported)
   ✓ Preprocessing complete.
```

---

## 🧪 ทดสอบด้วย test_data_cleaning.py

```bash
python test_data_cleaning.py
```

จะได้ผลลัพธ์:

```
🧪 ทดสอบฟังก์ชันทำความสะอาดข้อมูลตัวเลข (Accounting Format)
======================================================================

📊 ผลการทดสอบ:

                    Description         Original       Cleaned      Expected  Match
                  Normal number             1000       1000.00       1000.00   True
                     With comma            3,000       3000.00       3000.00   True
      With thousand separator        1,234,567    1234567.00    1234567.00   True
                   With decimal          3000.50       3000.50       3000.50   True
  Negative with parentheses            (3000)      -3000.00      -3000.00   True
...

✅ ผ่าน: 16/16 test cases (100.0%)
🎉 ทุก test cases ผ่านหมด!
```

---

## 📝 หมายเหตุ

### วงเล็บในระบบบัญชี
- `(3000)` = `-3000` (ค่าลบ)
- มักใช้ในรายงานทางบัญชี (Accounting Reports)
- ตัวอย่าง: Refund, Credit, ค่าปรับลด

### ค่า null/empty
- ค่าว่าง `""` → `0`
- `NaN` → `0`
- `None` → `0`

---

## 🔗 ไฟล์ที่เกี่ยวข้อง

- `main_audit.py:55-89` - ฟังก์ชัน `clean_numeric_column()`
- `crosstab_converter.py:200-224` - ฟังก์ชัน `clean_numeric_value()`
- `test_data_cleaning.py` - ทดสอบฟังก์ชัน
- `crosstab_data_example.csv` - ตัวอย่างข้อมูลจริง

---

**สร้างโดย:** Claude
**วันที่:** 2025-01-18
**เวอร์ชัน:** v4.1 (Accounting Format Support)
